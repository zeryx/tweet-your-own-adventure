# Add your twitter developer API credentials here.
# Ensure that your consumer API keys and your access token & access token secret are set.
# Once finished, recommended to rename this file to 'credentials.yaml', otherwise update the path in main.py
credentials:
  consumerAPI:
    key:
    secret:
  access:
    token:
    secret:
